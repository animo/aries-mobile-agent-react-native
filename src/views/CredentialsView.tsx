import React from 'react'
import { BaseView } from './BaseView'

const CredentialsView: React.FC = (): React.ReactElement => {
  return <BaseView viewTitle="credentials" />
}

export { CredentialsView }
